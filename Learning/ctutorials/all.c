#include<stdio.h>

int main()
{
    int   a,b; 
     printf("enter num a ") ;
     scanf("%d",&a );
     printf("enter num b ") ;
     scanf("%d",&b );

      printf("a+b is %d\n ",a==b );
      printf("a-b is %d\n",a||b );
      printf("a*b is %d \n",a&&b );
      printf("a xor b is %d\n ",a^b );
      return 0 ;

}